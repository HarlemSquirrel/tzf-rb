#!/usr/bin/env bash

set -e

version=$(ruby -e 'require_relative "lib/tzf/version"; puts TZF::VERSION')

echo "Building gem version $version"

gem build

git tag --sign "v$version"

git push --tags

gem push /home/kmack/code/tzf-rb/tzf-$version.gem
